/**
 * Langfuse HTTP endpoints.
 *
 * Requests to Langfuse currently go through the `@langfuse/client` SDK, so
 * raw URL building is not needed in `client.ts`. This file is reserved for
 * any future hand-built calls (e.g. health checks, raw REST). Constants and
 * defaults live in `./constants`.
 */

export {};
